-r requirements.txt

# docs
markdown

# linting
ruff
mypy
django-stubs[compatible-mypy]
djangorestframework-stubs[compatible-mypy]

# tests
pytest

# utils
python-dotenv # to load env files for mypy vscode extension